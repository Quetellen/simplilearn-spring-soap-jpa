#Tomcat Server
server:
  port: 8081
  error.include-stacktrace: always

#Logger
logging.level:
  web: DEBUG
  '[com.simplilearn.spring]': DEBUG

spring:
#Jackson Settings
  jackson:
    date-format: "yyyy-MM-dd"
    
#Database settings
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: Astro78*llOvw67%
  jpa:
    show-sql: true
    properties:
      hibernate:
        ddl-auto: validate
        '[format_sql]': true
        dialect: org.hibernate.dialect.MySQLDialect
        naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy